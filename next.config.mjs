/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  basePath: "/semantic-zoom-reader",
  images: { unoptimized: true },
};

export default nextConfig;
